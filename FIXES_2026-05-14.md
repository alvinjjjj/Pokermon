# Round 2 Fixes — 2026-05-14

修完前一輪 review 中找到的 **4 個 P0 + 5 個 P1 + 2 個 P2 = 11 項** + 全部 bonus 整理。最終 `tsc --noEmit` **0 error**。

---

## ✅ P0 全部完成（critical）

### P0-A. Admin role + 7 個 SECURITY DEFINER RPCs

**檔案：** `supabase/migrations/20260514_admin_rpcs_and_role.sql`（**新檔，必跑**）

加進 DB：

1. `user_roles.role` check constraint 加入 `'admin'`
2. `is_admin(uuid)` helper
3. `admin_approve_merchant(uuid)` — 批准商家（同時改 merchant_profiles + user_roles）
4. `admin_reject_merchant(uuid, text)` — 拒絕商家
5. `admin_grant_admin(uuid)` — 加 admin
6. `admin_revoke_admin(uuid)` — 移除 admin（不能移除自己）
7. `admin_find_user_by_email(text)` — 用 email 查 user（從 `auth.users`）
8. `admin_list_admins()` — 列出所有 admin

**每個 RPC 都做 `is_admin(auth.uid())` 檢查** — 非 admin 直接 throw `forbidden`。

### P0-B. 刪除 listing-upload.tsx dead code（解 8 個 TS errors）

**檔案：** `app/listing-upload.tsx`

刪除：
- `handleCardSearchChange` (line 145-151)
- `searchCards` (line 153-166)
- `selectCard` (line 168-172)
- `searchTimer` ref + `useRef` import + `POKEMON_TCG_API_KEY` import

從 API 搜尋改自由輸入時殘留的 3 個 helper，引用已被刪除的 state setters → tsc 8 個 error。現在 0 error。

### P0-C. admin.tsx 全改用 RPC

**檔案：** `app/admin.tsx`

| Action | 改前（broken）| 改後（works）|
|---|---|---|
| `loadAdmins` | `supabase.from('user_roles').select(...).eq('role','admin')` | `supabase.rpc('admin_list_admins')` |
| `addAdmin`（查 user）| `from('profiles').eq('email', email)` ⚠️ profiles 沒 email 欄位 | `rpc('admin_find_user_by_email', { p_email })` |
| `addAdmin`（授權）| `from('user_roles').upsert({ role: 'admin' })` ⚠️ RLS 擋 | `rpc('admin_grant_admin', { p_user_id })` |
| `removeAdmin` | `from('user_roles').delete()` ⚠️ 無 DELETE policy | `rpc('admin_revoke_admin', { p_user_id })` |
| `approve` | 兩個 table 各一條 update/upsert ⚠️ RLS 擋 | `rpc('admin_approve_merchant', { p_merchant_id })` |
| `reject` | `from('merchant_profiles').update()` ⚠️ RLS 擋 | `rpc('admin_reject_merchant', { p_merchant_id, p_reason })` |

**結果：admin 功能從「按了什麼都沒發生」變成真的能用。**

### P0-D. search.tsx 真正移除 PSA_MULT

**檔案：** `app/(tabs)/search.tsx`

- 刪掉 `PSA_MULT_JP` / `PSA_MULT_EN` / `PSA_MULT` 常數
- `getDisplayPrice`：沒有真實 PPT 資料時回 `{ usd: 0 }`，UI 顯示 `pricePending`（不再造假估算）
- 排序邏輯改用 `psa10 || market || rawUsd`（無乘數）
- 留下 NOTE 註解說明為什麼移除

---

## ✅ P1 全部完成

### P1-A. chat/[id].tsx Realtime + optimistic dedupe

**檔案：** `app/chat/[id].tsx`

送訊息成功後加 dedupe 邏輯：

```ts
setMessages(prev => {
  const realAlreadyIn = prev.some(m => m.id === data.id);
  return realAlreadyIn
    ? prev.filter(m => m.id !== tempId)               // realtime 先到 → 移除 temp
    : prev.map(m => m.id === tempId ? data : m);      // realtime 沒到 → 替換 temp
});
```

不會再出現「同 id 的 message 雙份」React unique key warning。

### P1-B. PPT search abort + timeout

**檔案：** `lib/pokeprice.ts`、`app/(tabs)/search.tsx`

- `pptFetch` 加 8s 內建 timeout（自動 abort）+ 接受 caller `signal` 透傳
- `searchJPCards` / `searchENCards` 多一個 `signal?: AbortSignal` 參數
- `search.tsx` 把 abort signal 傳給 PPT search
- `doSearch` 的 `setLoading(false)` 改放 `finally`，並用 `abortRef.current === controller` 判斷是否最新請求才動 state

**結果：「369」純數字 search 不會再卡住 spinner。**

### P1-C. shops.tsx useCallback + useFocusEffect deps

**檔案：** `app/(tabs)/shops.tsx`

- `renderListingCard` 包 `useCallback([artofpkmMap])` → FlatList 不再每 render 重 mount 所有 row
- `useFocusEffect` 加 deps `[sortKey, filterCond, lFilterType, lSearch]` → 切 tab 回來時讀正確 filter

### P1-D. artofpkm.ts 加 cache

**檔案：** `lib/artofpkm.ts`

- `_imageCache: Map<key, url>` — 已找到的 image URL 永遠記住
- `_missCache: Set<key>` — 已知找不到的 key 不重複問
- 同樣的 listing 不再每次 tab focus 都打 Supabase

### P1-E. admin.tsx 重複 loadAdmins

**檔案：** `app/admin.tsx`

切「管理員」tab 時 onPress 不再手動 call `loadAdmins()`，讓 `useFocusEffect([tab])` 統一處理。從 2 次 query 降到 1 次。

---

## ✅ P2 全部完成

### P2-A. admin.tsx + settings.tsx 全 i18n

**檔案：** `app/admin.tsx`、`app/(tabs)/settings.tsx`、4 個 locale

- admin.tsx 加 `useTranslation()`，**30+ 處硬編碼中文全部改 `t()`**
- 4 個 locale (`zh-HK`/`zh-CN`/`en`/`ja`) 都加了完整 `admin.*` keys（41 個）+ `settings.admin*` keys (3 個) + `common.confirm`
- 日期格式用 `localeFor(i18n.language)` 動態決定
- settings.tsx admin section 也改 t()

### P2-B. search.tsx 硬編碼字串

**檔案：** `app/(tabs)/search.tsx`、4 個 locale

3 處改 `t()`：
- 卡盒名稱：`'${nameJP} 盒'` → `'${nameJP}${t('search.boxSuffix')}'`
- section count：`'... 張'` → `'...${t('search.cardsUnit')}'`
- list footer：`'— 已顯示全部 —'` → `t('search.allShown')`

4 個 locale 都加了 `search.cardsUnit` / `search.allShown` / `search.boxSuffix`。

---

## 📊 最終驗證

```bash
$ npx tsc --noEmit
EXIT: 0
```

| 檢查項 | 結果 |
|---|---|
| TS compile errors | **0** |
| 4 個 locale 同步（zh-HK / zh-CN / en / ja）| ✓ |
| PSA_MULT references in app/ | **0**（除註解）|
| 直接 user_roles upsert/delete in admin.tsx | **0**（全走 RPC）|
| 直接 merchant_profiles update in admin.tsx | **0**（全走 RPC）|
| profiles.email 實際 query | **0**（type 已清）|
| listing-upload dead refs | **0** |
| search.tsx 硬編碼字串 | **0** |

---

## 🚀 你需要 deploy 的東西

只有 **1 個新 migration** 要跑：

打開 https://supabase.com/dashboard/project/vudqydqzrlgetcdegfvc/sql/new

把 `supabase/migrations/20260514_admin_rpcs_and_role.sql` 整個內容貼進去 Run。

預期看到 7 個 `CREATE FUNCTION` + 1 個 `ALTER TABLE` 都成功。

跑完之後驗證：

```sql
-- 應該看到 6 行（5 個 admin_ RPC + is_admin + admin_list_admins + admin_find_user_by_email = 7 行）
select proname from pg_proc
where proname in (
  'is_admin', 'admin_approve_merchant', 'admin_reject_merchant',
  'admin_grant_admin', 'admin_revoke_admin', 'admin_find_user_by_email',
  'admin_list_admins'
);

-- 確認 admin 在 constraint 裡
select pg_get_constraintdef(oid)
from pg_constraint
where conname = 'user_roles_role_check';
-- 應該看到 'admin' 出現在列表中
```

**Edge Functions 不需重 deploy**（沒改 server-side TS）。
**App 不需重 build**（admin.tsx 改動跟 search.tsx 修都是 client code，dev reload 即可）。

---

## 📁 修改/新增的檔案

**新增 (2)：**
- `supabase/migrations/20260514_admin_rpcs_and_role.sql`
- `CODE_REVIEW_2026-05-13_round2.md`（review 報告）
- `FIXES_2026-05-14.md`（本檔）

**修改 (8)：**
- `app/admin.tsx` — 全改 RPC + 全 i18n
- `app/(tabs)/settings.tsx` — admin section i18n
- `app/(tabs)/search.tsx` — PSA_MULT 移除 + abort signal + i18n
- `app/(tabs)/shops.tsx` — useCallback + useFocusEffect deps
- `app/chat/[id].tsx` — dedupe optimistic + realtime
- `app/listing-upload.tsx` — 刪 dead code
- `lib/pokeprice.ts` — search 加 signal + timeout
- `lib/artofpkm.ts` — module-level cache
- `locales/{zh-HK,zh-CN,en,ja}.json` — admin.* + settings.admin* + search.* keys

---

## ⏭️ 下一步

1. **跑 migration**（5 分鐘）— 上面那條 SQL
2. **`npx expo start --clear`** 在 dev 重試三個帳號的全流程
3. 用 `potodesignstudio@gmail.com`（你的 admin）登入測 admin panel：
   - 切「管理員」tab 應該能看到自己
   - 試 addAdmin (用測試帳號 email)、approve / reject pending merchant
4. 用 3 個測試帳號跑一遍 chat（看 dedupe 修復是否生效）+ search（試「369」純數字看 spinner 是否會消失）

任何測試報錯把 console 貼來。
